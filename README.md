Site de notícias sobre paleontologia construído com HTML, CSS, Javascript e EJS para cadeira de programação do técnico em informática.

Instale o node.js e os arquivos do site, após isso entre no terminal e digite **npm install** para instalar as dependências.

Após isso digite no terminal **npm start** para abrir o servidor.

Entre no navegador e digite **localhost:3000**.

